/*
    JavaScript에서는 모든 값이 truthy / falsy 를 갖는다.
    truthy : 참 같은 
    falsy  : 거짓 같은
    
    truthy : falsy를 제외한 모든 값
    falsy  : false, 0, "", NaN, null, undefined
    
    Boolean() 함수로 반환 여부 확인 가능

    모든 문장에 논리 연산을 사용할 수 있다.
    && (AND)     || (OR)      !(NOT)

    Boolean("")     // false
    Boolean(234)    // true
    


*/
